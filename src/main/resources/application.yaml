spring:
  application:
    name: mothramxbe

  datasource:
    url: jdbc:postgresql://localhost:5432/mothramx_db
    username: postgres
    password: s?Yf9THrm7

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect