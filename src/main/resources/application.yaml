spring:
  application:
    name: mothramxbe

  datasource:
    url: jdbc:postgresql://localhost:5432/mothramx_db
    username: postgres
    password: s?w

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect